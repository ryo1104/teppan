<%= javascript_pack_tag 'raty' %>
<% @page_title = @topic.title %>
<% breadcrumb :show_topic, @topic %>
<div class="page-body-container">
  <p><%= breadcrumbs separator: "  &rsaquo;  ", :class => "breadcrumbs custom" %></p>
  <div class="row">
    <div class="col s12 m8 topic-show">
      <div class="indent-wrapper">
        <h1 class="h1_class">トピック</h1>
      </div>
      <%= render partial: "topics/topicbody" %>
    </div>
  </div>
  <div class="row">
    <div class="col s12 m8 topic-show">
      <div class="indent-wrapper">
        <h1 class="h1_class">投稿ネタ(<%= @netas.count %>)</h1>
      </div>
      <% if @netas %>
        <ul class="collection z-depth-1">
        <%= render partial: "netas/neta", :collection => @netas %>
        </ul>
      <% else %>
        <div class="indent-wrapper">
          <p> (投稿はまだありません) </p>
        </div>
      <% end %>
      <% if user_signed_in? %>
        <div class="fixed-action-btn horizontal">
          <%= link_to new_topic_neta_path(@topic.id), {:class => 'btn-floating light-green accent-4'} do %>
            <i class="material-icons">create</i>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col s12 m8 topic-show">
      <div class="indent-wrapper">
        <h1 class="h1_class">トピックへのコメント(<%= @comments.count %>)</h1>
      </div>
      <% unless @comments %>
        <div class="indent-wrapper">
          <p> (投稿はまだありません) </p>
        </div>
        <% if user_signed_in? %>
          <ul class="collection z-depth-1">
            <%= render partial: "topics/newcomment" %>
          </ul>
        <% end %>
      <% else %>
        <ul class="collection z-depth-1">
          <%= render partial: "topics/comment", :collection => @comments %>
          <% if user_signed_in? %>
            <%= render partial: "topics/newcomment" %>
          <% end %>
        </ul>
      <% end %>
    </div>
  </div>
</div>
