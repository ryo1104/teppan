<div class="page-body">
  <div class="row">
    <div class="col s12 m9 neta-new">
      <h1 class="h1_class">購入結果</h1>
      <ul class="collection z-depth-1 user-page">
        <li>
          <%= @message %><br><br>
          価格　：<%= @trade_inputs[1]["price"] %>円<br>
          消費税：<%= @trade_inputs[1]["ctax"] %>円<br>
          <b>合計　：<%= @trade_inputs[1]["charge_amount"] %>円</b><br><br>
          <%= link_to neta_path(@tradeable.id), {:class => 'btn btn-primary withripple custom' } do %>
            <i class="material-icons"></i>ネタを表示
          <% end %>
          <br><br>
        </li>
      </ul>
    </div>
  </div>
</div>