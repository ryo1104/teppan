<div class="page-body-container">
  <div class="row">
    <div class="col s12 m9 neta-new">
      <h1 class="h1_class">有料プラン</h1>
      <ul class="collection z-depth-1 user-page">
        <div class="page-body-container">
          有料プランのお申し込みが完了しました。<br><br>
          <% if @subscription_hash["trial_end"].present? %>
            無料体験終了：<%= Time.zone.at(@subscription_hash["trial_end"].to_i).strftime("%Y/%m/%d") %><br>
          <% end %>
          請求日：<%= Time.zone.at(@subscription_hash["current_period_end"].to_i).strftime("%Y/%m/%d") %><br>
          請求金額：<%= @subscription_hash["plan"]["amount"] %>円<br>
          請求先カード：<%= @card_hash["brand"] %>&nbsp;&nbsp;
                        <%= @card_hash["exp_month"] %>/<%= @card_hash["exp_year"] %>&nbsp;&nbsp;
                        *******<%= @card_hash["last4"] %><br>
          <br>
          <% if false %>
          customer_hash = <%= @customer_hash %><br><br>
          subscription_hash = <%= @subscription_hash %><br><br>
          <% end %>
        </div>
      </ul>
    </div>
  </div>
</div>